# LOOP_LOG

loop-dev（ループ型／張り付き型 開発）の振り返り記録。検証済みの事実のみを「学び」に残す。

| 日付 | 機能 | 反復数 | 無介入完了率 | 概算コスト | 人間レビュー | 学び（要点） |
|---|---|---|---|---|---|---|
| 2026-06-25 | WORKS詳細「ケアGO」追加（一覧＋詳細ページ全ブロック＋Figma実画像8枚） | 張り付き（/loop未使用）。縦スライス1＋本文一括2バッチ＋画像差し替え＝コミット計6本 | N/A（張り付き）。`pp:compare` は幾何で7/7自動緑。見た目はユーザー2ラウンドの指摘（画質/写真比率・改行）で微修正 | 1セッション | Preview確認＋2ラウンド | 推測せずMCP要素実測／足場(pp:compare)を先に緑実証／縦スライスで経路確認／ブロック別width上書きで列幅一致／画像もMCP書き出し／幾何は機械・改行/絵柄は人間 |

---

## 2026-06-25 ケアGO 詳細ページ — 詳細メモ

### 効いた手順
1. 適性判定（機械判定 pass/fail を作れるか）→ 条件付き○
2. `pp:compare`（決定論的差分Lint）を**実走して緑を実証**してから量産（足場ゲート）。素のままでは未稼働だったので playwright install / 実体 npm ci / .env.local が必要だった
3. **縦スライス1本（hero）**で足場が緑になる経路を確認 → 残りに展開
4. テキストは Figma `get_design_context` を**要素ごとに実測**し、**明示改行（`<p>`区切り）を `string[]`** で再現
5. ブロックごとに異なる Figma 列幅（1279/1304/1520/740）を **任意 `width` 上書き**で固定（他ページ不変＝surgical）
6. 画像は Figma MCP の `localhost:3845/assets` から実書き出し可（image-fillノードのみ）

### 精度が出た核心
「最初の1要素から細かく実測」＋「機械で数値検証できる足場を先に通す」。前回は画像の印象から推測してズレたが、今回は推測を排した。

### 正直だった点 / 軌道修正
- 当初「MENTE 同型の軽作業」と見立てたが、実際は列幅が各様・写真比率の罠ありで見立てより大きめ。**縦スライスで早期に実態を掴み軌道修正**できた（最初に全見積もりせず1本通す効用）
- 写真の「縦伸び」は歪みではなく cover トリミング。原因は書き出し写真(3:2)と枠(1.637)の比率不一致。支給の実比率写真で解消

### 関連メモリ
`project_pp_compare_scaffold_proven` / `feedback_works_detail_per_block_width` / `reference_figma_mcp_image_export`
