# =============================================================
# お問い合わせフォーム送信先（どちらか / 両方を設定）
# =============================================================

# ── 1) WordPress + Contact Form 7 経由（本番想定） ──
# 両方が設定されている時は CF7 を優先して使用する。
NEXT_PUBLIC_WORDPRESS_API_URL=https://your-wp-domain.com
CF7_FORM_ID=123

# ── 2) Resend 直送（CF7 未接続時のフォールバック） ──
# 上記 CF7 のどちらかが未設定の場合のみ、こちらが使われる。
RESEND_API_KEY=re_xxxxxxxxxxxxxxxxxx

# =============================================================
# ニュース（WordPress 連携・予定）
# =============================================================
# NEXT_PUBLIC_WORDPRESS_API_URL を共用
